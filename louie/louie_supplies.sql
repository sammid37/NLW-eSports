-- alter table supplies modify column grade varchar(255); 
-- insert into supplies (id, url, name, description, brand, grade, price) 
-- values (1, "url","aquarela","descricao", "marca", "artist", 22.9);
-- alter table supplies add category varchar(255);
-- SET SQL_SAFE_UPDATES = 0;

show databases;
use louie;

describe supplies;
select * from supplies;

